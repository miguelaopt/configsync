/** The landing page brings its own header and footer (see page.tsx); this layout adds nothing. */
export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
